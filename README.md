# Module M206 - Création d'une application Cloud Native
Ce repository contient tous les TPs organisés par branches.
- main : Structure de base
# Créer le package.json (le -y répond "oui" à tout par défaut)
npm init -y

# Installer Express
npm install express

# Installer Nodemon en dépendance de développement (très utile pour le Cloud Native)
npm install --save-dev nodemon